<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.tba.theboxingapp.NotificationFragment">

    <TextView
        android:id="@+id/empty_notification_item"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="You do not have any notifications yet. Try making some comments!"
        android:textSize="18sp"
        android:gravity="center"
        android:visibility="invisible"
        android:paddingLeft="20dp"
        android:layout_marginTop="150dp"
        android:paddingRight="20dp"
        >
    </TextView>

    <ListView
        android:id="@+id/notificationsList"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        />


    <ProgressBar
        android:id="@+id/loadNotificationProgress"
        style="@android:style/Widget.Holo.Light.ProgressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:visibility="invisible"
        />
</RelativeLayout>
